export const PXBanner = (function () {
	'use strict';

	function init() {
		console.log('Initialize PXBanner block');

		window.addEventListener('load', (ev) => {
		});
	}

	return {
		init: init,
		name: 'banner',
	};
})();
